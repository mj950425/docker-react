sudo: required

language: generic

services:
  - docker

before_install:
  - echo "start Creating an image with dockerfile"
  - docker build -t minjoon/docker-react -f Dockerfile.dev ./

script:
  - docker run -e CI=ture minjoon/docker-ract npm run test -- --coverage

after_success:
  - echo "Test Success"